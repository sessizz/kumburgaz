[phases.setup]
nixPkgs = ["dotnet-sdk", "postgresql_17"]

[phases.build]
cmds = [
  "dotnet restore Kumburgaz.Web.csproj",
  "dotnet publish Kumburgaz.Web.csproj -c Release -o out"
]

[start]
cmd = "dotnet out/Kumburgaz.Web.dll"
